/**
 * @description 231-2 的幂 test
 * @author 氧化氢
 */

import { isPowerOfTwo } from "./index";

describe('231-2 的幂', () => {
  it('示例一', () => {
    const n = 1
    const res = isPowerOfTwo(n)
    expect(res).toBe(true)
  })

  it('示例二', () => {
    const n = 1
    const res = isPowerOfTwo(n)
    expect(res).toBe(true)
  })

  it('示例三', () => {
    const n = 1
    const res = isPowerOfTwo(n)
    expect(res).toBe(true)
  })

  it('示例四', () => {
    const n = 1
    const res = isPowerOfTwo(n)
    expect(res).toBe(true)
  })

  it('示例五', () => {
    const n = 1
    const res = isPowerOfTwo(n)
    expect(res).toBe(true)
  })
})